# 潮汐孤岛（CCGame）

一个基于 **Vanilla JavaScript + Vite** 的 2D 生存建造网页游戏。玩家会出生在一片程序生成的群岛中，白天采集资源、制作工具和建造设施，夜晚抵御从海岸出现的敌人，尽可能延长生存天数并提高分数。

## 项目特点

- **程序化群岛地图**：使用噪声与岛屿描述符生成大世界，并按区块动态加载。
- **生存循环完整**：包含生命、饥饿、口渴、体力、昼夜变化与天气提示。
- **采集 / 制作 / 建造**：可采集木材、石块、纤维、浆果、椰子等资源，制作石斧、石镐、长矛、鱼竿、篝火、木墙、木地板和雨水收集器。
- **战斗与防守**：夜晚会出现“暗潮爬行者”，需要通过武器和营地布局进行防守。
- **交互式 UI**：包含背包、制作面板、海图、世界目标面板、右键物品菜单和提示文本。
- **钓鱼系统**：可对近处水面抛竿，在咬钩窗口内收线获得不同鱼类。

## 当前玩法

1. 开始游戏后先在岛上采集树木、岩石、灌木和棕榈。
2. 通过右侧制作面板合成基础工具与建造套件。
3. 左键切换当前手持物，靠近目标后进行采集、攻击、建造或使用。
4. 使用篝火补充夜间营地收益，利用木墙和地板搭建防线。
5. 建造雨水收集器补充饮水，或通过鱼竿在浅水 / 深水区域捕鱼。
6. 在夜晚击退敌人，尽可能维持角色状态并提高生存评分。

## 操作说明

- `W / A / S / D`：移动
- `Shift`：冲刺
- `鼠标左键`：行动 / 攻击 / 使用当前手持
- `鼠标右键（场景）`：取消当前手持
- `E`：与当前目标交互
- `背包左键`：切换当前手持物品
- `背包右键`：打开物品菜单
- `Esc`：关闭物品菜单

## 本地运行

### 环境要求

- Node.js 20+（GitHub Pages 工作流使用 Node 20）
- npm

### 安装依赖

```bash
npm ci
```

### 启动开发环境

```bash
npm run dev
```

启动后按 Vite 默认提示访问本地地址即可。

### 构建生产版本

```bash
npm run build
```

构建产物输出到 `dist/` 目录。

## 项目结构

```text
CCGame/
├─ index.html              # 页面结构与 HUD / 弹层
├─ styles/                 # 全局样式
├─ src/
│  ├─ app/                 # 启动、输入绑定、主循环
│  ├─ components/          # 地块、资源、敌人、结构定义
│  ├─ core/                # 常量、状态、DOM 引用
│  ├─ data/                # 物品与配方数据
│  ├─ ecs/                 # 简单 ECS 核心
│  ├─ inventory/           # 背包与物品操作
│  ├─ render/              # 场景绘制与特效
│  ├─ systems/             # 行为、运行时与工厂系统
│  ├─ ui/                  # 面板、菜单、提示交互
│  ├─ world/               # 地图生成与世界辅助逻辑
│  └─ workers/             # 区块生成 Worker
├─ vite.config.mjs         # Vite 配置
└─ .github/workflows/      # GitHub Pages 部署流程
```

## 技术说明

- 使用原生 Canvas 进行渲染。
- 通过模块拆分组织 ECS、渲染、UI、世界生成与物品系统。
- 使用 Web Worker 处理世界区块生成，降低主线程压力。
- 使用 Vite 进行开发与构建，`base: './'` 方便静态部署。

## 部署

仓库已包含 GitHub Pages 工作流：

- 推送到 `main` 分支后自动执行 `npm ci` 与 `npm run build`
- 构建完成后发布 `dist/` 到 GitHub Pages

## 后续可扩展方向

- 增加更多敌人、生态与岛屿事件
- 扩展建筑功能与营地自动化
- 增加音效、存档、成就与新手引导
- 增加更细分的昼夜 / 天气玩法反馈
